<UserControl x:Class="HotelManagement.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             FontFamily="/Resources/GothamLight.otf#Gotham Light"
             FontSize="15"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:HotelManagement.Views"
             xmlns:viewModels="clr-namespace:HotelManagement.ViewModels">

    <Grid>
        <TabControl Margin="0">

            <!-- Pregled Rezervacija -->
            <TabItem Header="Reservations">
                <Border Grid.Row="0" Margin="20">
                    <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow" ItemsSource="{Binding Reservations}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding ReservationID}" Header="Reservation ID" IsReadOnly="True" />
                            <DataGridTextColumn Binding="{Binding CheckInDate}" Header="Check-in Date" />
                            <DataGridTextColumn Binding="{Binding CheckOutDate}" Header="Check-out Date" />
                            <DataGridTextColumn Binding="{Binding NumberOfGuests}" Header="Number of Guests" />
                            <DataGridTextColumn Binding="{Binding RoomID}" Header="Room ID" />

                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.DeleteReservationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" 
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            ToolTip="Delete" Height="20"
                                            Foreground="#C70000"
                                            Margin="0" Padding="0">
                                            <Button.Content>
                                                <materialDesign:PackIcon Kind="Delete" Width="16" Height="16" />
                                            </Button.Content>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <!-- Pregled Računa -->
            <TabItem Header="Invoices">
                <Border Grid.Row="0" Margin="20">
                    <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow" ItemsSource="{Binding Invoices}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding InvoiceID}" Header="Invoice ID" IsReadOnly="True" />
                            <DataGridTextColumn Binding="{Binding TotalAmount}" Header="Total Amount" />
                            <DataGridTextColumn Binding="{Binding IssuedDate}" Header="Issued Date" />
                            <DataGridTextColumn Binding="{Binding GuestID}" Header="Guest ID" />
                            <DataGridTextColumn Binding="{Binding ReservationID}" Header="Reservation ID" />
                            
                            
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.DeleteInvoiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" 
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            ToolTip="Delete" Height="20"
                                            Foreground="#C70000"
                                            Margin="0" Padding="0">
                                            <Button.Content>
                                                <materialDesign:PackIcon Kind="Delete" Width="16" Height="16" />
                                            </Button.Content>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
